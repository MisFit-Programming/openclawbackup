# 2026-04-03

- User (Philip S.) requested and approved creation of `at-time-entry` skill for Autotask/MSP technician time-entry cleanup; skill created at `skills/at-time-entry/SKILL.md` and packaged as `at-time-entry.skill`.
- Dobbie used repo-local git identity `Philip <philip.stacy@gmail.com>` and committed workspace as root commit `9e5a40e` with message `Add at-time-entry skill for MSP technician time entries`.
- Installed OpenClaw `gog` skill and configured Linux `gog` CLI with Google OAuth for account `philip.stacy@gmail.com`; verified working with calendar access and Gmail label listing.
- User’s Gmail custom labels discovered and used for automation: `1: To Respond`, `2: FYI`, `3: Comment`, `4: Notifications`, `5: Meeting Update`, `6: Awaiting Reply`, `7: Actioned`, `8: Finance`, `9: Family / School`, `10: Health / Medical`, `11: Home / Utilities`, `12: Security / Logins`, `13: Taxes / Legal`, `14: Travel / Leisure`, `15: Subscriptions / Digital Services`, `16: Promotions / Noise`.
- Built Gmail labeling automation in workspace scripts: `scripts/label_gmail_inbox.py` and `scripts/run_label_gmail_inbox.sh`; scheduled recurring job `gmail:auto-label-inbox` every 30 minutes for `philip.stacy@gmail.com` to label unlabeled inbox threads using only custom labels.
- Per user request, performed host/OpenClaw health review and hardening actions: removed public Caddy reverse proxy exposing OpenClaw dashboard, then stopped and disabled Caddy entirely. OpenClaw remains bound to loopback (`127.0.0.1:18789` and `127.0.0.1:18791`), with public dashboard exposure removed.
- User still needs dashboard access; recommended SSH local port forward pattern: `ssh -L 18789:127.0.0.1:18789 <user>@164.92.110.113` and then browse to `http://127.0.0.1:18789`.
- Scheduled daily health/update reporting jobs targeting Slack channel `C0AQ69U1YG7` (`#appupdates`): `healthcheck:security-audit` daily at 19:00 UTC (11:00 PST year-round) and `healthcheck:update-status` daily at 19:05 UTC.
- Added Slack allowlist entries for channels `C0AQ69U1YG7` and `C09C91KVD88` in OpenClaw config; gateway config reload/restart behavior has been finicky, but gateway remained healthy and jobs were created.
- Scheduled CTO/MSP reporting jobs targeting Slack channel `C09C91KVD88`: `briefing:cto-msp-daily` daily at 15:30 UTC (07:30 PST) and `watch:standard-msp-outages` every 30 minutes for standard MSP vendor outage alerting.
- User prefers Slack reports to use Block Kit rather than plain mrkdwn. Saved compact Block Kit templates in `templates/slack/`: `cto_msp_daily_brief.blockkit.json`, `msp_outage_alert.blockkit.json`, and `openclaw_health_report.blockkit.json`.
- Built Block Kit scaffold scripts and examples in workspace: `scripts/render_blockkit.py`, `scripts/prepare_slack_blockkit_payload.py`, `scripts/slack_blockkit_sender_README.md`, plus sample contexts and rendered payloads under `examples/blockkit-contexts/` and `examples/blockkit-output/`.
- Important implementation note: OpenClaw Slack path appears to support internal Block Kit, but there is not yet a confirmed documented native cron/announce path for arbitrary raw `blocks` payloads. Next likely step is inspecting OpenClaw internal reply-block format for native integration rather than using raw Slack API calls.
- User likes assistant persona “Dobbie” as playful/warm/lightly flirty (non-explicit), and generated a realistic image representing Dobbie.
- User asked for regular CTO/MSP briefs, outage alerts, daily security/update checks, and ongoing Gmail auto-labeling; these are now important standing workflows/preferences.
- Outage watch profile refined for user’s Lodi, CA base: include both local and global alerts, suppress duplicates, and explicitly monitor major infrastructure plus property-management and CPA/accounting software commonly used by clients.
- Added assistant security guardrails covering prompt-injection handling for web content, outbound secret redaction, DM-only financial handling, approval gates for email/public actions and drafts, and automated security cron jobs for nightly codebase review, weekly gateway verification, monthly memory scanning, and daily repo size monitoring.
# 2026-04-03

- User (Philip S.) requested and approved creation of `at-time-entry` skill for Autotask/MSP technician time-entry cleanup; skill created at `skills/at-time-entry/SKILL.md` and packaged as `at-time-entry.skill`.
- Dobbie used repo-local git identity `Philip <philip.stacy@gmail.com>` and committed workspace as root commit `9e5a40e` with message `Add at-time-entry skill for MSP technician time entries`.
- Installed OpenClaw `gog` skill and configured Linux `gog` CLI with Google OAuth for account `philip.stacy@gmail.com`; verified working with calendar access and Gmail label listing.
- User’s Gmail custom labels discovered and used for automation: `1: To Respond`, `2: FYI`, `3: Comment`, `4: Notifications`, `5: Meeting Update`, `6: Awaiting Reply`, `7: Actioned`, `8: Finance`, `9: Family / School`, `10: Health / Medical`, `11: Home / Utilities`, `12: Security / Logins`, `13: Taxes / Legal`, `14: Travel / Leisure`, `15: Subscriptions / Digital Services`, `16: Promotions / Noise`.
- Built Gmail labeling automation in workspace scripts: `scripts/label_gmail_inbox.py` and `scripts/run_label_gmail_inbox.sh`; scheduled recurring job `gmail:auto-label-inbox` every 30 minutes for `philip.stacy@gmail.com` to label unlabeled inbox threads using only custom labels.
- Per user request, performed host/OpenClaw health review and hardening actions: removed public Caddy reverse proxy exposing OpenClaw dashboard, then stopped and disabled Caddy entirely. OpenClaw remains bound to loopback (`127.0.0.1:18789` and `127.0.0.1:18791`), with public dashboard exposure removed.
- User still needs dashboard access; recommended SSH local port forward pattern: `ssh -L 18789:127.0.0.1:18789 <user>@164.92.110.113` and then browse to `http://127.0.0.1:18789`.
- Scheduled daily health/update reporting jobs targeting Slack channel `C0AQ69U1YG7` (`#appupdates`): `healthcheck:security-audit` daily at 19:00 UTC (11:00 PST year-round) and `healthcheck:update-status` daily at 19:05 UTC.
- Added Slack allowlist entries for channels `C0AQ69U1YG7` and `C09C91KVD88` in OpenClaw config; gateway config reload/restart behavior has been finicky, but gateway remained healthy and jobs were created.
- Scheduled CTO/MSP reporting jobs targeting Slack channel `C09C91KVD88`: `briefing:cto-msp-daily` daily at 15:30 UTC (07:30 PST) and `watch:standard-msp-outages` every 30 minutes for standard MSP vendor outage alerting.
- User prefers Slack reports to use Block Kit rather than plain mrkdwn. Saved compact Block Kit templates in `templates/slack/`: `cto_msp_daily_brief.blockkit.json`, `msp_outage_alert.blockkit.json`, and `openclaw_health_report.blockkit.json`.
- Built Block Kit scaffold scripts and examples in workspace: `scripts/render_blockkit.py`, `scripts/prepare_slack_blockkit_payload.py`, `scripts/slack_blockkit_sender_README.md`, plus sample contexts and rendered payloads under `examples/blockkit-contexts/` and `examples/blockkit-output/`.
- Important implementation note: OpenClaw Slack path appears to support internal Block Kit, but there is not yet a confirmed documented native cron/announce path for arbitrary raw `blocks` payloads. Next likely step is inspecting OpenClaw internal reply-block format for native integration rather than using raw Slack API calls.
- User likes assistant persona “Dobbie” as playful/warm/lightly flirty (non-explicit), and generated a realistic image representing Dobbie.
- User asked for regular CTO/MSP briefs, outage alerts, daily security/update checks, and ongoing Gmail auto-labeling; these are now important standing workflows/preferences.
- Outage watch profile refined for user’s Lodi, CA base: include both local and global alerts, suppress duplicates, and explicitly monitor major infrastructure plus property-management and CPA/accounting software commonly used by clients.
- Added assistant security guardrails covering prompt-injection handling for web content, outbound secret redaction, DM-only financial handling, approval gates for email/public actions and drafts, and automated security cron jobs for nightly codebase review, weekly gateway verification, monthly memory scanning, and daily repo size monitoring.
- Built importable n8n maintenance workflow file at `n8n/nightly-dev-maintenance-blockkit.json` for Docker-based self-monitoring with Slack Block Kit output, health checks, Watchtower checks, unattended-upgrades checks, and actionable fix commands in alerts.
- Learned actual n8n Docker container name is not stable/simple (`n8n-docker-caddy-n8n-1` on the current host), so monitoring logic should detect containers by name/image match rather than exact hardcoded `n8n`.
- Diagnosed Watchtower failure on the n8n host as Docker API mismatch (`client version 1.25 is too old; minimum supported API version is 1.40`); n8n itself was healthy while Watchtower needed recreation/update.
- Fixed Gog for non-interactive runs by ensuring its keyring password/env is available to shell-driven sessions; Gog calendar access works again for automation and scheduling tasks.
- User wakes around 5 AM most days and prefers key briefings like the CTO/MSP brief earlier than 7:30 AM; this is a durable scheduling preference.
- Slack operating model was tightened in live config: mention-only in allowed channels, Philip-only user allowlist (`UTC1ELSBU`), and 👀 acknowledgment reaction for group mentions.
- Created Slack channel/routing spec at `docs/slack-communication-layout.md` with strict single-purpose channels, no cross-posting by default, and DM/private-only handling for financial content.
- User created the first core Slack channel set (daily brief, outages, healthcheck, cron failures, assistant admin, plus private financial destination likely pending); routing was later wired after channel IDs were captured.
- Added dedicated Autotask ticket analysis lane/channel `C0AR5BWR7GR`, with production spec, two dedicated Block Kit templates, and a daily 2:00 AM America/Los_Angeles cron job `analysis:autotask-ticket-daily` for webhook-driven ticket analysis using company IDs only and separate maintenance metrics.
